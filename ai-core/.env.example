# Environment Variables Example
# Copy this file to .env and add your actual values

# GitHub Personal Access Token (Classic)
# Required for scripts that access private repositories
# Create one at: https://github.com/settings/personal-access-tokens
#   1. Click "Generate new token" → "Generate new token (classic)"
#   2. Check: repo (Full control of private repositories)
#   3. Generate and copy the token (starts with ghp_)
# Permissions needed: repo (full control of private repositories)
GITHUB_TOKEN=your_github_token_here

# Other environment variables (if needed)
# EXAMPLE_VAR=value
